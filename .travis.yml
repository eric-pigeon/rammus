language: ruby

rvm:
  - 2.6

gemfile:
  - Gemfile

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

before_install:
  - gem update --system

script: "bundle exec rspec"
